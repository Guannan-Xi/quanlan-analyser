# Legacy Static MVP

The previous static prototype remains in:

```text
outputs/eeglab-mne-mvp
```

It is useful as a visual reference and demo asset source, but formal development now happens in:

```text
frontend/
backend/
worker/
eeg_core/
data/
docs/
```

